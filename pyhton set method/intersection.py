# Define two sets
set_A = {1, 2, 3, 4, 5}
set_B = {3, 4, 5, 6, 7}

# Using intersection() to get elements common to both sets
common_elements = set_A.intersection(set_B)

print("Common elements between set_A and set_B:", common_elements) #output Common elements between set_A and set_B: {3, 4, 5}
